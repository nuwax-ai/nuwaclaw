//! 根组件

/// 根组件 - 主布局容器
pub struct Root {
    // TODO: 实现 UI 组件
}

impl Root {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for Root {
    fn default() -> Self {
        Self::new()
    }
}
