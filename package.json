{
  "private": true,
  "scripts": {
    "prepare": "husky",
    "test:electron": "cd crates/agent-electron-client && npm run test:run"
  },
  "devDependencies": {
    "husky": "^9",
    "lint-staged": "^16",
    "prettier": "^3.8.1"
  },
  "lint-staged": {
    "crates/agent-tauri-client/src/**/*.{ts,tsx}": [
      "prettier --write",
      "pnpm --filter agent-tauri-client lint"
    ],
    "crates/agent-electron-client/src/**/*.{ts,tsx}": [
      "prettier --write"
    ]
  }
}
